{
  "name": "@quantis/shared",
  "version": "1.0.0",
  "description": "Shared types, constants, and utilities for the Quantis crypto platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
